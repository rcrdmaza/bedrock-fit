# involve.me-style landing template

Snapshot of the full involve.me-style homepage (July 2026) before the modal-calculator rework.

- page.tsx — full landing page: nav, hero, inline calculator, how-it-works, features, stats band, archetypes, FAQ, CTA band, footer, ad slots, AvatarWindow
- globals.css — matching global styles (lp-* landing classes + bf-* calculator grids)
- AvatarWindow.tsx — floating strength-bot widget used by this version

To restore: copy page.tsx to src/app/page.tsx and merge the lp-* / bf-* blocks of globals.css.
